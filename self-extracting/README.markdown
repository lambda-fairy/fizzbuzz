Self-extracting FizzBuzz
========================

A shell script containing the compressed output.  When run, a minimal
stub extracts the data and pipes it to stdout.


Running
-------

You will need a Unix system with `xz` installed.

    $ ./build
    $ ./fizzbuzz
